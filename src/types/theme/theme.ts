export type Theme = {
    colors: {
        background: string;
        text: string;
        primary: string;
        border: string;
        surface: string;
        placeholder: string;
        inputBackground: string;
    };
};