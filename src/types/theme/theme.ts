export type Theme = {
    colors: {
        background: string;
        text: string;
        primary: string;
        border: string;
        surface: string;
        card: string;
        textSecondary: string;
        placeholder: string;
        inputBackground: string;
        primaryText: string;
    };
};